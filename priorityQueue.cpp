#include"priorityQueue.h"
#include"PQ_entry.h"
void priority_queue::makeNull()
{
    last = 0;
}
void priority_queue::insertInPQ(PQ_entry x)
{
    int i;
    PQ_entry temp;
    if (last>=MAXSIZE)
    {
    }
}
